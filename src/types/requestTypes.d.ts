type OriginalUrl = {
  originalUrl: string;
};

type TargetLinkId = {
  targetLinkId: string;
};

type LinkParam = {
  targetLinkId: string;
};

type ProAdminUser = {
  userId: string;
};

type UserLinkID = {
  LinkId: string;
};

type DeleteLinkRequest = {
  targetUserId: string;
  targetLinkId: string;
};

type UserIdParam = {
  targetUserId: string;
};
